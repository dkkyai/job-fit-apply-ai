# Gradle
.gradle/
build/
bin/
Task
build_status.txt
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db

# Environment files
.env
.env.*
!build.gradle.kts
token.json
tokens/
dkkytech_credentials.json
*_credentials.json

# Logs
*.log

# Output directory
output/

# Steel Browser persisted session state (cookies/localStorage for LinkedIn/Glassdoor) — secret.
state/

# Personal data — populated by `--init-profile`. Templates ship in source.
config/candidate_profile.yaml
config/candidate_profile.json
src/main/resources/resume/resume.yaml
src/main/resources/resume/generated_resume.html
src/main/resources/resume/*-generated.html
# Personal résumé YAML exports + local LaTeX byproducts in the resume dir.
# The committed toolchain (resume_template.tex.jinja, yaml_to_tex.py, fonts/) ships in
# source; any other YAML/TeX/PDF here is personal or a manual-run artifact.
src/main/resources/resume/*.yaml
!src/main/resources/resume/resume.template.yaml
src/main/resources/resume/*.tex
src/main/resources/resume/*.pdf
# Backups generated by --init-profile (date-stamped suffix)
*.bak

# Node (generated by npm install -g in CI scripts)
node_modules/
package-lock.json
.kilo
